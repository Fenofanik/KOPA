
import 'package:flutter/material.dart';
import 'colors.dart';

final nonShadow =  ButtonStyle(
overlayColor: MaterialStateProperty.all(transparent),
);

final signUp =  ButtonStyle(
  overlayColor: MaterialStateProperty.all(forButtons),
);